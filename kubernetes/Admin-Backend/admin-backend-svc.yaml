apiVersion: v1
kind: Service
metadata:
  labels:
    app: admin-backend
  name: admin-backend-svc
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: http
    nodePort: 32003
  selector:
    app: admin-backend
  type: NodePort