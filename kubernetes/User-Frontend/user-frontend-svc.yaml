apiVersion: v1
kind: Service
metadata:
  labels:
    app: user-frontend
  name: user-frontend-svc
  namespace: user-apps
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: http
    nodePort: 32001
  selector:
    app: user-frontend
  type: LoadBalancer